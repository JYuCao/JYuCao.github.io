# JYCao.github.io

曹家宇的个人主页与技术博客，使用 Astro 构建并部署到 GitHub Pages。

## 本地开发

```bash
npm install
npm run dev
```

## 检查与构建

```bash
npm run build
```

构建产物位于 `dist/`。推送到 `master` 后，GitHub Actions 会自动构建并发布网站。
